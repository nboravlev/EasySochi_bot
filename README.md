# EasySochi Booking Bot 🏠

Telegram-бот для посуточной аренды жилья в Сочи. Полнофункциональная платформа для бронирования, управления объектами и коммуникации между собственниками и арендаторами.

---

### 👥 Роли пользователей

- **Админ**: Полный доступ к управлению системой и статистике.
- **Арендатор**: Поиск жилья по датам/параметрам, бронирование, отмена и чат с владельцем.
- **Собственник**: Добавление объектов, управление календарем занятости, аналитика и общение с клиентами.

---

## 🛠 Технологии

- **Core**: `Python 3.12` (библиотека `python-telegram-bot`)
- **Database**: `PostgreSQL 15` + `PostGIS` (геопозиционирование)
- **ORM**: `SQLAlchemy` + `Alembic` (миграции)
- **Infrastructure**: `Docker`, `Docker Compose`, `Nginx Gateway`
- **Monitoring**: Встроенный Web Log Viewer (FastAPI)

--

##  Быстрый запуск

> Убедитесь, что установлены Docker и Docker Compose

1. Клонируйте репозиторий:
```bash
git clone https://github.com/nboravlev/EasySochi_bot.git .
```
2. Создайте файл `.env` на основе примера:
```bash
cp .env.example .env

```
3. Запустите приложение

```
docker compose up -d --build
```

## 🏗 Структура проекта
```
EasySochi_bot/
├── bot/                # Исходный код бота (handlers, schemas, utils)
│   ├── alembic/        # Миграции базы данных
│   ├── main.py         # Точка входа
│   └── entrypoint.sh   # Скрипт инициализации и ожидания БД
├── db/                 # Конфигурация PostgreSQL и расширений
├── log_viewer/         # Веб-интерфейс для просмотра логов в реальном времени
├── docker-compose.yml  # Описание сервисов и томов
└── README.md
```

## 🛡 Безопасность и архитектура
* Изоляция: Бот работает в изолированной Docker-сети.
* Env Vars: Конфиденциальные данные (токены, пароли БД) вынесены в переменные окружения и не попадают в репозиторий.
* Logs: Доступ к логам осуществляется через защищенный Gateway на отдельном порту.

## 📈 Планы развития
* Веб-интерфейс админа (FastAPI + React).
* Интеграция с платежными шлюзами (ЮKassa / Payture).
* Глобальная экосистема EasySochi: бронирование экскурсий и услуг внутри одного бота.
* Расширенная аналитика каналов трафика.

---

## Лицензия и правила использования

Этот проект распространяется под лицензией MIT – подробности см. в файле [LICENSE](./LICENSE).
